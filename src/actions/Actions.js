import * as types from '../constants/ActionTypes';

/*
Each action defined in constants/ActionTypes.js is bound to a function here.
Example:

  export function addTodo(name) {
    return {
      type: types.ADD_TODO,
      name
    };
  }
*/
